CREATE TABLE [dbo].[ApparelStore] (
    [Name]  VARCHAR (50) NULL,
    [ID]    INT          NOT NULL,
    [Type]  VARCHAR (50) NULL,
    [Price] INT          NOT NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC)
);

